version: '3.8'

services:
  mysql-db:
    image: mysql:8
    container_name: mysql_renta_movil
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: renta_movil
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: